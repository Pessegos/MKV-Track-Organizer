Version 1.0.5 improves the Audio Sync workspace layout and background stream-loading feedback.

- Summary and Raw log now receive more vertical space by default than the source-track export table.
- The divider remains draggable for manual adjustment.
- Automatic stream loading reports through the status bar without animating the global progress bar before analysis starts.
