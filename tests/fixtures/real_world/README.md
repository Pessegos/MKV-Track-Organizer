# Real-world regression fixtures

These files contain reduced, sanitized metadata derived from media layouts that
previously exposed Organizer bugs. They contain no video, audio, subtitle text,
or disc data.

Each fixture records only the track properties needed to reproduce the original
decision and the expected stable result. Keep the movie/case name because it
makes a failing regression immediately recognizable.
